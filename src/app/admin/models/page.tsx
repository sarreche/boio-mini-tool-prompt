import { requireAdmin } from "@/lib/admin/auth";import { getPrivateData } from "@/lib/admin/data";import { PageHeader,button,field } from "../ui";import { saveModelRoute } from "../actions";
type ModelData={providers:Array<{id:string;name:string;code:string;is_active:boolean}>;models:Array<{id:string;provider_id:string;display_name:string;code:string;is_active:boolean}>;routes:Array<{id:string;model_id:string;route_code:string;priority:number;is_active:boolean}>};
export default async function ModelsPage(){const context=await requireAdmin();const data=await getPrivateData<ModelData>(context,"models");return <><PageHeader title="Modelos y fallback" description="Orden efectivo por ruta; nunca puede quedar una ruta sin modelos activos."/><div className="space-y-3">{data.routes.map(route=>{const model=data.models.find(m=>m.id===route.model_id);const provider=data.providers.find(p=>p.id===model?.provider_id);return <form action={saveModelRoute} key={route.id} className="flex flex-wrap items-center gap-3 rounded-2xl border border-[#d9dee7] bg-white p-5"><input type="hidden" name="id" value={route.id}/><div className="min-w-72 flex-1"><p className="font-bold">{provider?.name} / {model?.display_name}</p><p className="text-xs text-[#60708f]">{route.route_code} · {model?.code}</p></div><label className="text-sm">Prioridad <input className={`${field} w-24`} type="number" name="priority" defaultValue={route.priority}/></label><select name="isActive" defaultValue={String(route.is_active)} className={field}><option value="true">Activo</option><option value="false">Inactivo</option></select><button className={button}>Guardar</button></form>})}</div></>;}
