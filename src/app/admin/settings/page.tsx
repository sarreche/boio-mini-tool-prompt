import { requireAdmin } from "@/lib/admin/auth";import { getPrivateData } from "@/lib/admin/data";import { PageHeader,button,field } from "../ui";import { saveSetting } from "../actions";
export default async function SettingsPage(){const context=await requireAdmin(true);const settings=await getPrivateData<Record<string,unknown>>(context,"settings");return <><PageHeader title="Configuración" description="Umbrales operativos globales administrados exclusivamente por root."/><div className="space-y-3">{Object.entries(settings).map(([key,value])=><form action={saveSetting} key={key} className="rounded-2xl border border-[#d9dee7] bg-white p-5"><input type="hidden" name="key" value={key}/><label className="font-bold">{key}</label><div className="mt-3 flex gap-2"><input className={`${field} flex-1 font-mono`} name="value" defaultValue={JSON.stringify(value)}/><button className={button}>Guardar</button></div></form>)}</div></>;}
